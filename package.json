{
  "name": "bataknese-wedding-budget",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd backend && npx prisma generate",
    "start": "cd backend && npm start",
    "server": "cd backend && npm start",
    "postinstall": "cd backend && npm install && npx prisma generate",
    "dev": "echo 'Run backend and frontend separately — see README'"
  }
}
