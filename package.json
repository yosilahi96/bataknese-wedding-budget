{
  "name": "bataknese-wedding-budget",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "postinstall": "npm run install:all && npx prisma generate --schema=backend/prisma/schema.prisma && npm run build",
    "dev": "echo 'Run backend and frontend separately — see README'"
  }
}
